// Home route should show the main recommendations experience immediately.
// We reuse the existing recommendations page component.
import RecommendationsPage from './recommendations';

export default RecommendationsPage;